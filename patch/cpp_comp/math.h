#ifndef __LIBCPP_MATH_COMPLEMENT__
#define __LIBCPP_MATH_COMPLEMENT__

#include "math.h"

namespace std {
    inline string to_string(int val) {
        return to_string(val);
    }

    inline double nearbyint(double x) {
        return nearbyint(x);
    }
    
    inline constexpr float
    nearbyint(float x) {
        return nearbyint(x);
    }

    inline constexpr long double
    nearbyint(long double x) {
        return nearbyint(x);
    }
}

#endif